<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIEngine.Avalonia.Views;assembly=UIEngine.Avalonia"
        xmlns:node="clr-namespace:UIEngine;assembly=UIEngine">
  <!--
  <Styles.Resources>
    <DataTemplates x:Key="PatternMatching">
      <DataTemplate DataType="{x:Type node:ObjectNode}">
        <local:ObjectBox ObjectNode="{Binding ElementName=Me, Path=NextNode}"/>
      </DataTemplate>
    </DataTemplates>
  </Styles.Resources>
  <Style Selector="ContentControl.Next">
    <Setter Property="ContentTemplate">
      <Setter.Value>
        
      </Setter.Value>
    </Setter>
  </Style>
  -->
</Styles>